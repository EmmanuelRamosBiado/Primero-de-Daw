public class Kumite{
  public static boolean block(int[] attackers, int[] defenders){
	  
	  int atacantes[];
	  
	  int defensores[];
	  
	  if(attackers.length > defenders.length) {
		  return false;
	  } else {
		  return true;
	  } 
	  
	  if(attackers.length == defenders.length) {
		  
	  }
	  
	  
		  
	  
	  
    
    }
  }